# Arch Forge CLI

Um gerador de projetos de microsserviços em Python (FastAPI + DDD).

## Instalação (Desenvolvimento)
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

## Uso
```bash
arch-forge --help
```
